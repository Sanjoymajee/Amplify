<!DOCTYPE html>
<html>
  <head>
    <title>Profile</title>
    <!-- Include Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/style/profile.css" />
  </head>
  <body>
    <div class="container">
      <h1>Profile</h1>
      <div class="row">
        <div class="col-md-6">
          <div class="form-group">
            <label for="username">Username:</label>
            <p><%= profileUser.username %></p>
          </div>
          <div class="form-group">
            <label for="email">Email:</label>
            <p><%= profileUser.email %></p>
          </div>
          <% if(user && user.username !== profileUser.username) { %>
              <div class="mt-5">
                <a href="/chat/<%= profileUser.username %>" class="btn btn-primary">Chat</a>
              </div>
              <form id="requestForm" method="POST" action="/friends/sendRequest">
                <input type="hidden" name="friendUsername" value="<%= profileUser.username %>">
                <button type="submit">Send Friend Request</button>
              </form>
            <% } %>
          <div class="mt-5">
            <a href="/" class="btn btn-primary">Back to Home</a>
          </div>
        </div>
      </div>
    </div>

    <!-- Include Bootstrap JS -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
  </body>
</html>
